#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * main - prints name.
 * @argc: first argument
 * @argv: second argument
 * Return: always return 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
