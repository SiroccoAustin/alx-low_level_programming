#include <stdlib.h>
#include <stdio.h>

/**
 * main - prints name.
 * @argc: first argument
 * @argv: second argument
 * Return: always return 0
 */

int main(argc, argv[])
{
	printf("%s", argv[0]);
	return (0);
}
