#include "main.h"

/**
 * is_prime_number - outputs prime number
 * @n: integer argument
 * Return: prime number
 */


int is_prime_number(int n)
{
	if (n % 2 == 1)
	{
	return (1);
	}
	if (n == 1)
	{
	return (0);
	}
	return (0);
}
