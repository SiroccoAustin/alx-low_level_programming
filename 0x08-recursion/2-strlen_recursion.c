#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen_recursion - outputs length of the string
 * @s: string
 * Return: length of the string
 */

int _strlen_recursion(char *s)
{
	return (strlen(s));
}
