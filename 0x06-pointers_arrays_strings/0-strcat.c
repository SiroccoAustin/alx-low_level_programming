#include "main.h"
#include <string.h>

/**
 * _strcat - returns a combination of two strings
 * @dest: first argument
 * @src: second argument
 * Return: a combination of strings
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
